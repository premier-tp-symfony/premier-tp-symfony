{% extends 'base.html.twig' %}

{% block title %}
    {{ product.name }} - {{ parent() }}
{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-6">

        </div>
        <div class="col-md-6">
            <h1 class="display-4">{{ product.name }}</h1>
            <p>Prix : {{ product.price / 100 }} &euro;</p>
        </div>
    </div>
{% endblock %}